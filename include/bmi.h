#ifndef BMI_H
#define BMI_H

typedef float Kilograms;
typedef float Pounds;
typedef float Meters;
typedef float Inches;

float calculate_bmi_metric(Kilograms, Meters);
float calculate_bmi_imperial(Pounds, Inches);

#endif // BMI_H
